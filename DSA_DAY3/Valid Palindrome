class Solution(object):
    def isPalindrome(self, s):
        t = ""
        for i in s:
            if i.isalnum():
                t=t+i
        t=t.lower()
        if t==t[::-1]:
            return True
        else:
            return False
